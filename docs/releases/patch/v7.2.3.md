# v7.2.3 (Patch Release)

**Status**: Released

This is a new patch release of the `github-actions` package. It fixes issues with the package in a way that should require no refactoring. Please read below the description of changes.

## Description of Changes

- Renames the pull request branch created in `create-pull-request-templates` workflow.
  - I forgot to change this when copying over from `create-feature-docs` initially...
- Add the build step to the above workflow.
  - alex-c-line may need this as it gets its configs from its build.